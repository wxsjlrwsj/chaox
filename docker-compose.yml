name: chaoxing-system
services:
  mysql:
    image: mysql:8.0
    container_name: chaoxing-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: chaoxing
    volumes:
      - mysql_data:/var/lib/mysql
    command: ["--default-authentication-plugin=mysql_native_password"]
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-proot"]
      interval: 10s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
    container_name: chaoxing-backend
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      DB_URL: jdbc:mysql://mysql:3306/chaoxing?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8
      DB_DRIVER: com.mysql.cj.jdbc.Driver
      DB_USER: root
      DB_PASSWORD: root
      SECURITY_TOKEN_SECRET: change-me-prod
      SECURITY_CORS_ALLOWED_ORIGINS: http://localhost:8080
    ports:
      - "8083:8083"

  frontend:
    build:
      context: ./frontend
    container_name: chaoxing-frontend
    depends_on:
      - backend
    ports:
      - "8080:8080"

volumes:
  mysql_data:
